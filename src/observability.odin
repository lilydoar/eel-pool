package game

// Purpose: A separate process that watches the app process 
// Monitor for application process that does not shutdown cleanly
// Process Manager to restart the app if it crashes
// Track proper cleanup of tagged/registered resources
// Has a role that watches the game database: were all transactions completed? did cleanup serialization complete successfully?

